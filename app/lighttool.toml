[tool]
id = "com.lightphone.audiobooks"
label = "Audiobooks"
versionCode = 17
versionName = "0.5.6"
permissions = []
# The tool is a thin UI; the companion (:server) hosts the SDK service, the
# /sdcard/Audiobooks scan, and playback. Point at com.lightos on a real LP3
# once Light ships the media methods there.
serverPackage = "com.lightphone.audiobooks.server"
orientation = "portrait"
